@model EndpointMvc.Models.ParamInfo

<div>
	<h4>@Model.Name <span class="endpoint-type small">@Model.Type</span></h4>
	<div>@Model.Description</div>
	@if( Model.Default != null ) {
		<div>@Model.Default</div>
	}
	@Html.CheckBoxFor(m => m.Optional)
</div>